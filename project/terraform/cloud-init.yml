#cloud-config
users:
 - name: ivon
   groups: sudo
   shell: /bin/bash
   sudo: ['ALL=(ALL) NOPASSWD:ALL']
   ssh-authorized-keys:
      - "ssh-ed25519 ************************** ivon@ivon-VBox"
